import { AlertTriangleIcon } from 'lucide-react';

/**
 * Placeholder banner — lot-based expiry prediction removed with the
 * simplified ledger architecture.  Re-implement when per-package expiry
 * tracking is added back.
 */
export function ExpiryWarningBanner(_props: { prediction?: unknown }) {
  return null;
}
